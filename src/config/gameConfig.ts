export const GAME_CONFIG = {
    width: 800,
    height: 600,
    moveStep: 10,
};

export const MAP_BOUNDS = {
    minX: 0,
    minY: 0,
    maxX: GAME_CONFIG.width,
    maxY: GAME_CONFIG.height,
};
